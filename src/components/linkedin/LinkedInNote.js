import React from "react";

const LinkedInNote = props => {
  return (
    <div className="linkedinNote">
      <p>Allow RSME to access LinkedIn profile:</p>
      <p className="note">
        Plase note that RSME only use the information to generate online resume.
      </p>
    </div>
  );
};

export default LinkedInNote;
